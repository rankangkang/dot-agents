interface:
  display_name: "Code Review"
  short_description: "Senior engineer code review: SOLID, security, performance, error handling"
  default_prompt: "Resolve the requested review scope (workspace, branch, or range), then review the resulting diff for SOLID violations, security risks, race conditions, error handling issues, performance problems, and boundary condition bugs."

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
